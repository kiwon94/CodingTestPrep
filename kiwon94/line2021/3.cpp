#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<vector<int>> jobs) {
    int cur_num;
    int cur_time;
    cur_time=
    for(int i=0;i<jobs.size();i++)
    {
        
    }
    vector<int> answer;
    return answer; 
}